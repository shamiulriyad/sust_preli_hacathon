# Required for LLM-backed reasoning. If unset, the service runs in rule-based-only mode.
ANTHROPIC_API_KEY=

# Optional overrides
ANTHROPIC_MODEL=claude-haiku-4-5-20251001
LLM_TIMEOUT_SECONDS=8.0

# Injected automatically by Render — do not set manually in production
PORT=8080
